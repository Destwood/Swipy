NEXT_PUBLIC_SUPABASE_URL=https://gsoomjcfkezysgletkag.supabase.co
# Prefer the dashboard "anon" / "publishable" public key (NOT service_role / secret)
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

IGDB_CLIENT_ID=
IGDB_CLIENT_SECRET=

# Public site URL. Used as a fallback when the OAuth callback cannot see the real host.
# Also set this as Site URL in Supabase → Authentication → URL Configuration.
# Redirect URLs should include: http://localhost:3000/** and https://<prod-domain>/**
NEXT_PUBLIC_SITE_URL=https://swipy-chi.vercel.app

# Google OAuth: enable in Supabase Dashboard → Authentication → Providers → Google
# (Client ID / Secret live in the dashboard, not in this file)
